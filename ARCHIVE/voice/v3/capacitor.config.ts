import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.casacompanion.app',
  appName: 'CasaCompanion',
  webDir: 'backend/client'
};

export default config;
